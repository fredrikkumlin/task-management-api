spring:
  application:
    name: "task-management-api"
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:tasksdb
    driverClassName: org.h2.Driver
    username: "sa"
    password: "password"
  jpa:
    hibernate:
      ddl-auto: update
  security:
    user:
      name: test
      password: test
      roles: USER

springdoc:
  api-docs:
    path: "/api-docs"
